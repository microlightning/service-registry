// if a proxy causes an error, it makes sense to return that error back to the requestor including the error code
// however we should wrap it to make all errors across the ecosystem consistent, regardless of how the develoer codes it
// consistency is key!
